{
  "name": null,
  "project": "agora",
  "category": "fix",
  "branchName": "chief/50-finetune-live-endpoints",
  "description": "Phase A (KFT completion), cross-repo. Un-404 the trainer's real output endpoints \u2014 the KFT \u00a76 training-telemetry stream, the \u00a75.3 export, and the \u00a78 registry registration \u2014 turning today's stubbed/404 surfaces into a live, subscribable stream. These endpoints only come alive when an orchestrator-side KCB finetune CLIENT replaces `Runner::Stub` (discover -> invoke -> subscribe to the \u00a76 stream -> issue `invoke:finetune` grants per \u00a77): the trainer's stream dials that runtime. agora's half is standing the endpoints up as real, subscribable surfaces and proving them with an in-repo KCB client that drives the four-verb dance the orchestrator will run. Cross-repo closure per ROADMAP Phase A, reconciled to current truth: the orchestrator-side client is `cuneiform:90-finetune-client` (merged be509c32 \u2014 `Runner::Kcb` replaced `Runner::Stub` in cuneiform-operate), and the specialized provider is LUGH, not pinakes \u2014 `pinakes:90-finetune-provider` was never authored (pinakes `90` became `90-extract-lugh`, and cuneiform `102-retarget-finetune-provider-to-lugh` records the retarget); the provider manifest is `lugh:30-kft-provider-manifest` (merged 985db18). Both closure deps are encoded in dependsOn below and are already satisfied. agora gates on `make check`; the trainer gates on `make check-trainer`.",
  "parked": false,
  "dependsOn": [
    "41-finetune-job-validator",
    "lugh:30-kft-provider-manifest",
    "cuneiform:90-finetune-client"
  ],
  "touches": [
    "trainer"
  ],
  "iters": 8,
  "warmup": [
    "make help 2>/dev/null | head -40",
    "sed -n '28,73p' trainer/README.md",
    "sed -n '120,135p' trainer/README.md",
    "ls trainer/src/agora_trainer",
    "sed -n '1,80p' ~/Development/koine/specs/fine-tuning.md"
  ],
  "verify": [
    "make check"
  ],
  "userStories": [
    {
      "id": "US-1",
      "title": "The \u00a76 training-telemetry stream goes live and subscribable",
      "description": "Turn the KFT \u00a76 training-telemetry from a stub/404 into a live stream over the `/invoke` request (KCB \u00a74 subscribe), one event per training step in monotonic order, content-addressed by `job + step` so redelivery is idempotent, closed by the terminal event carrying the minted finetuned-model id + weight/export asset ids (trainer/README.md \u00a7'Admission, engines & telemetry').",
      "acceptanceCriteria": [
        "`/invoke` streams real KFT \u00a76 telemetry (one idempotent, content-addressed event per step, terminal event carrying the minted model + asset ids) rather than returning a stub/404 \u2014 driven by the recorded-run adapter where no GPU is present (never a fabricated loss curve).",
        "A subscriber (KCB \u00a74 subscribe) receives the ordered event sequence to the terminal event; a test asserts monotonic ordering and idempotent redelivery; `make check-trainer` green."
      ],
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-2",
      "title": "Un-404 the \u00a75.3 export and \u00a78 registry registration",
      "description": "Make the \u00a75.3 export matrix (weights + each requested export as KMI assets with lineage) and the \u00a78 discovery-registry registration of the minted model entity live, replacing the 404s. Output-egress enforcement at registration (\u00a75.4/FT-A) must refuse a `local-only`-inheriting model a cross-boundary registration (`egress-output`).",
      "acceptanceCriteria": [
        "The \u00a75.3 export endpoint returns the real export matrix (KMI assets carrying `media:derived_from`/`media:variant_of` lineage) and the \u00a78 registration surface registers the minted model entity, both replacing prior 404s.",
        "Registration enforces the \u00a75.4/FT-A output-egress rule \u2014 a `local-only`-inheriting model is refused cross-boundary registration (`egress-output`), an in-tier one admits \u2014 proven by a test; `make check-trainer` green."
      ],
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-3",
      "title": "An in-repo KCB finetune client drives the four-verb dance end to end",
      "description": "Prove the live endpoints with an in-repo KCB client that runs discover -> invoke -> subscribe -> issue `invoke:finetune` grant \u2014 the same dance the orchestrator's production client (`cuneiform:90-finetune-client`, merged: `Runner::Kcb`) runs at deploy time. This is the agora-side standing proof; the production client is another repo's work.",
      "acceptanceCriteria": [
        "A test-scoped KCB finetune client discovers the trainer, invokes a schema-valid job, subscribes to the live \u00a76 stream to the terminal event, and carries an `invoke:finetune` grant token with a `X-Agora-Budget-Units` ceiling (\u00a77) \u2014 end to end against the live endpoints, no stub.",
        "The test documents itself as the agora-side stand-in for the orchestrator's production client (`cuneiform:90-finetune-client`), which dials the specialized lugh provider (`lugh:30-kft-provider-manifest`) + this trainer at runtime; `make check` green."
      ],
      "passes": true,
      "notes": ""
    }
  ]
}
