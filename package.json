{
  "name": "agora",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Runtime commons for the ecosystem — TypeScript areas (schemas, clients, registry, resolver, console).",
  "workspaces": [
    "schemas",
    "clients/*",
    "registry",
    "resolver",
    "console"
  ],
  "scripts": {
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "eslint .",
    "test": "npm run test --workspaces --if-present",
    "build": "npm run build --workspaces --if-present"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^22.20.1",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.46.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=22"
  }
}
