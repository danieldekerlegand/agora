{
  "name": "@agora/relation-registry-client",
  "license": "Apache-2.0",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Loader for the shared relation registry — fetches koine's vocabulary + bridge mappings and indexes them.",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@agora/schemas": "*"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  }
}
