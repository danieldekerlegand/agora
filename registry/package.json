{
  "name": "@agora/registry",
  "license": "Apache-2.0",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Thin KCB discovery registry — indexes manifests, answers with addresses, never proxies.",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@agora/sdk": "*",
    "@agora/schemas": "*"
  },
  "devDependencies": {
    "@agora/resolver": "*"
  },
  "scripts": {
    "start": "node src/main.ts",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "build:native": "node path-index/build-node-addon.mjs"
  }
}
